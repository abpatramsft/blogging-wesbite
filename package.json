{
  "name": "blogging-website",
  "version": "1.0.0",
  "description": "A locally deployable blogging website with an Express backend and a vanilla JS frontend. Posts persist as Markdown files under content/posts.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "marked": "^12.0.2"
  }
}
